---
import { ANALYTICS } from 'weekly:config';
---

{
  ANALYTICS?.umami?.id ? (
      <script is:inline async defer data-website-id={ANALYTICS.umami.id} src="https://umami.mengpeng.tech/script.js"></script>
    ) : null
}
