---
import {cn} from '~/utils/utils';
import Tags from './Tags.astro';
import dayjs from 'dayjs';
import {Icon} from 'astro-icon/components';

interface StatCardProps {
    title: string;
    date: string | Date;
    tags?: string[];
    desc?: string;
    cover?: string;
    showTags?: boolean;
    eyebrow?: string;
}

const {title, date, desc, tags = [], cover, showTags = true, eyebrow = 'Weekly'} = Astro.props as StatCardProps;

// 格式化日期
const formattedDate = new Date(date).toLocaleDateString('zh-CN', {
    month: 'short',
    day: 'numeric',
    year: 'numeric'
});

const isNew = dayjs(date).isAfter(dayjs().subtract(2, 'week'));
const defaultCover = '/images/weekly-default-cover.svg';
const coverSrc = cover || defaultCover;

const cardClasses = cn(
    'group relative h-full',
    'rounded overflow-hidden', // VSCode style: 4px radius
    'glass-panel', // Solid panel style
    'transition-all duration-200 ease-out',
    'hover:border-[#505050]', // Subtle border highlight on hover
    'cursor-pointer',
    'focus-within:ring-2 focus-within:ring-primary/50 focus-within:ring-offset-2 focus-within:ring-offset-background'
);
---

<div class={cardClasses}>
    <!-- Border highlight on hover -->
    <div
        class='absolute inset-0 rounded pointer-events-none border border-transparent group-hover:border-[#505050] transition-colors duration-200 z-20'>
    </div>

    {
        isNew && (
            <div class='absolute top-3 right-3 z-30'>
                <span class='inline-flex items-center px-2 py-0.5 rounded text-[10px] font-bold tracking-wider uppercase bg-primary text-white shadow-lg shadow-primary/20'>
                    New
                </span>
            </div>
        )
    }

    <!-- 内容区域 -->
    <div class='relative z-10 h-full flex flex-col'>
        <!-- Cover image -->
        <div class='relative aspect-[16/9] overflow-hidden'>
            <div
                class='absolute inset-0 bg-gradient-to-t from-black/60 to-transparent z-10 opacity-60 group-hover:opacity-40 transition-opacity duration-300'>
            </div>
            <img
                src={coverSrc}
                alt=''
                class='w-full h-full object-cover transition-transform duration-700 ease-out group-hover:scale-105'
                loading='lazy'
                decoding='async'
                onerror={`this.onerror=null;this.src='${defaultCover}';`}
            />
        </div>

        <div class='flex flex-col flex-1 p-5 gap-3'>
            <!-- Eyebrow -->
            <div class='flex items-center gap-2 text-xs text-muted-foreground/80'>
                <div class='w-1.5 h-1.5 rounded-full bg-primary shadow shadow-primary/50'></div>
                <span class='font-medium uppercase tracking-wider'>{eyebrow}</span>
            </div>

            <!-- Title -->
            <h3
                class='text-lg font-bold text-foreground leading-snug line-clamp-2 group-hover:text-primary transition-colors duration-200'>
                {title}
            </h3>

            <!-- Content -->
            <div class='mt-auto space-y-4'>
                {
                    desc && (
                        <p class='text-sm text-muted-foreground leading-relaxed line-clamp-2 group-hover:text-muted-foreground/80 transition-colors'>
                            {desc}
                        </p>
                    )
                }

                {
                    showTags && tags.length > 0 && (
                        <div class='flex flex-wrap gap-1.5 pt-1'>
                            <Tags tags={tags} maxVisible={3} variant='minimal' size='sm' compact />
                        </div>
                    )
                }

                <!-- Footer -->
                <div class='flex items-center justify-between pt-3 border-t border-border text-sm'>
                    <time datetime={date.toString()} class='text-muted-foreground/70 font-mono text-xs'
                        >{formattedDate}</time
                    >
                    <span
                        class='inline-flex items-center gap-1.5 text-muted-foreground group-hover:text-primary transition-colors duration-200'>
                        <span class='text-xs font-semibold'>阅读</span>
                        <Icon
                            name='tabler:arrow-right'
                            class='w-3.5 h-3.5 transition-transform duration-200 group-hover:translate-x-1'
                        />
                    </span>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    /* Typography optimization */
    h3 {
        text-rendering: optimizeLegibility;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
        font-feature-settings: 'cv02', 'cv03', 'cv04', 'cv11';
    }

    /* Text truncation */
    .line-clamp-2 {
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }

    /* Reduced motion */
    @media (prefers-reduced-motion: reduce) {
        .group,
        .group * {
            transition: none !important;
            animation: none !important;
        }
    }

    /* Touch device optimization */
    @media (hover: none) {
        .group:hover {
            transform: none;
        }
    }

    /* High contrast mode */
    @media (prefers-contrast: high) {
        .group {
            border-width: 2px;
            background: hsl(var(--card));
        }
    }
</style>
